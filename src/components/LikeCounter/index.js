import React from "react";
import { WrapperLikeCounter } from "./units";

const LikeCounter = ({ number }) => {
  return <WrapperLikeCounter>{number}</WrapperLikeCounter>;
};

export default LikeCounter;
